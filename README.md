# JSON API Deep Compare

Instructions: 
1. Run `npm install` to install dependancies
2. Run `node compare.js <URLForSQLEndoint>` to compare the request body of the SQL and Dynamo endpoint call and how long the request took